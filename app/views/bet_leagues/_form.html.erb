<%= form_with(model: bet_league, local: true) do |form| %>
  <% if bet_league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bet_league.errors.count, "error") %> prohibited this bet_league from being saved:</h2>

      <ul>
        <% bet_league.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
